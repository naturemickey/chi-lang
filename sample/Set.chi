// 后台用C++实现更好一点。
struct Set[T] : (ss : T...) {
	val _map : Map[T, Char] = Map(ss.map(s -> (s => _PRESENT)));
	lambda contains : T -> Boolean = key -> _map.get(key, (k, v) -> true, k -> false);
	lambda size : Int = _map.size;
	ambda forEach : (T); 
	lambda map; // 未实现
	lambda filter; // 未实现
	lambda reduce; // 未实现
}
val Char _PRESENT = 'p';

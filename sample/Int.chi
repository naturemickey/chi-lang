
native struct Int {
	lambda add : Int -> Int;
	lambda + = add;
	lambda sub : Int -> Int;
	lambda - = sub;
	lambda muti : Int -> Int;
	lambda * = muti;
	lambda div : Int -> Int;
	lambda / = div;
	lambda mod : Int -> Int;
	lambda % = mod;
	lambda > : Int -> Bool;
	lambda < : Int -> Bool;
	lambda >= : Int -> Bool;
	lambda <= : Int -> Bool;

	lambda < : Int -> Bool = i -> self - i < 0
	lambda <= : Int -> Bool = i -> self - i <= 0;
	lambda > : Int -> Bool = i -> i < self;
	lambda >= : Int -> Bool = i -> i <= self;
	
	lambda toFloat : Float;
	lambda parseInt : String -> Int;
}
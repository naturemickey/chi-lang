// enum是Chi的讲法糖
// enum Color {RED, YELLOW, BLUE} 相当于
// native struct Color;
// struct _EnumColor {
//	val RED = Color();
//	val YELLOW = Color();
//	val BLUE = Color();
// }
// val enumColor = _EnumColor();
// 
// Color的本地实现“建议”为自增长的int，这样性能会比较好（也可以直接使用引用的地址，而不需要指向任何对象）。
// 在语法层面：
// 1.不同的enum是不同的类型，互相不能比较。
// 2.同一个enum的不同对象的值都是不同的，如果比较==会返回false，没有其它比较运算。
//
// 对于enum，Chi会有一个语法糖：
// 在case...when...逻辑结构中，when部分必须覆盖这个enum的所有对象，不能有else部分。这是为了避免遗漏（未来对这个enum的项有任何修改，则所有这类语句如果不改就会直接报错）。